mutation CreateMessage ($id: ID!, $content: String!) {
  createMessage(id: $id, content: $content) {
    id
    content
    createdAt
    
    author {
      id
      username
      avatar
    }
  }
}