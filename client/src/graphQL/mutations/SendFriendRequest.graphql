mutation SendFriendRequest ($username: String!) {
  createFriendRequest(username: $username) {
    id 
    createdAt
    updatedAt
    to {
      id
    	username
    }
    
    from {
      id
    	username
    }
  }
}