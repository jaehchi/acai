query messages($id: ID!) {
  messages(id: $id, orderBy: createdAt_ASC) {
    id
    content
    createdAt

    author {
      id
      username
      avatar
    }
  },
  channel(id: $id) {
    id
    name
  }
}
