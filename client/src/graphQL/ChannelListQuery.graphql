query ($id: ID!) {
  channels(id: $id, orderBy: type_ASC) {
    id
    name
    type
    position

    children {
      id
      name
      type
      position
    }
  },
  guild (id: $id) {
    id
    name
  }
}