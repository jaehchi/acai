# import * from './generated/prisma.graphql'

type Mutation {
  createUser(username: String!, email: String!): Users
  createGuild(guildname: String!, owner: String!): Guilds
  joinGuild(guild_id: String!, user_id: String!): Guild_members
}

type Query {
  user(id: ID!): Users
  users: [Users!]!
}