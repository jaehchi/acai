projects:
  database:
    schemaPath: schema/generated/prisma.graphql
    extensions:
      prisma: prismaLayer/prisma.yml
  app:
    schemaPath: schema/index.graphql
    extensions:
      endpoints:
        default: http://localhost:4000